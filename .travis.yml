language: node_js
node_js:
  - 0.10
before_install: gem install compass
before_script:
  - cd app
  - npm install -g grunt-cli
  - npm install -g bower
  - bower install
  - bower update
  - bower prune
  
