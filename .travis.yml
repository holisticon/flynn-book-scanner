language: node_js
sudo: false
node_js:
  - 0.12
before_install:
  - npm install -g grunt-cli
before_script:
  - pwd
  - cd app
  - npm install
  - grunt build
script:
  - pwd
  - grunt check
  - grunt test
